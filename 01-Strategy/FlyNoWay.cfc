component displayName="FlyNoWay" implements="FlyBehavior" {
    
    // flying behavior implementation for ducks that do NOT fly (like rubber ducks and decoy ducks)

    public string function fly() {
        return "I can't fly";
    }

}